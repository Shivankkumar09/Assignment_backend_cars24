# Server
PORT=3000
NODE_ENV=development
LOG_LEVEL=info

# SQLite file (relative to process cwd unless absolute)
DATABASE_PATH=./data/cars24_ops.db

# Gemini — required for chat. The engine also accepts GOOGLE_API_KEY.
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_MODEL=gemini-2.0-flash
